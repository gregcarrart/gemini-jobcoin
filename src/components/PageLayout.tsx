import styled from 'styled-components';

const PageLayout = styled.div`
    width: 100%;
    max-width: 1440px;
    margin: 90px auto;
    padding: 0 30px;
    position: relative;
`;

export default PageLayout;
